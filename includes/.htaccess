# Nothing in this directory should ever be fetched directly over HTTP —
# it holds database credentials and server-side logic.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
